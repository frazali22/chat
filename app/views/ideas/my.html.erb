
<h1><%= current_user.name %></h1>
<p><%= image_tag(@user.picture_url, :width => 100) if @user.picture.present? %></p>
<p><strong></strong><a><%= current_user.email %></strong></a>.</p>
<%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
<%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
<p class="article-title">

<h1>User Profile</h1>

<div class="pro-set">
  <h1>Name : </h1>  <h3 class="pro-h3"><%= current_user.name %> </h3>
  <h1>Father Name : </h1><h3 class="pro-h3"><%= current_user.fname %></h3>
  <h1>Email : </h1><h3 class="pro-h3"><%= current_user.email %> </h3>
  <h1>Contact : </h1><h3 class="pro-h3"><%= current_user.contact %></h3>
  <h1>Enrollment : </h1><h3 class="pro-h3"><%= current_user.enrollment %></h3>
  <h1> Session : </h1><h3 class="pro-h3"><%= current_user.session %></h3>
  <h1>Department : </h1><h3 class="pro-h3"><%= current_user.department %> </h3>
  <h1>Address : </h1><h3 class="pro-h3"><%= current_user.address %></h3>
  <h1>Work at : </h1><h3 class="pro-h3"><%= current_user.work_place %></h3>
  <h1> Work Experience : </h1><h3 class="pro-h3"><%= current_user.work_experience %></h3>
  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link btn btn-large btn-block btn-success' %>
</div>

  <% if @posts.present? %>
    <h1>All Result Listed Here-my</h1>
    <% @posts.each do |article| %>
        <div class="article">
          <h1 class="article-title"><%= image_tag(article.picture_url, :width => 100) if article.picture.present? %><%= article.name %></h1>
          <p class="article-content"><%= article.email %><%= article.enrollment %><%= article.contact %></p>
          <%= link_to "Send Message", "#", class: "btn btn-success btn-xs start-conversation",
                  "data-sid" => current_user.id, "data-rip" => article.id %>
              </div>

          <%  end %>
          <% else %>

          </p>
          <% if (params[:search]) %>
              <h1>All Result Listed Here</h1>
              <p>There are no name in :db <%= params[:search] %>.</p>
          <% end %>
    <% end %>






